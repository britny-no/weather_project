{
  "name": "weather",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "export NODE_ENV=prod && node ./dist/server.js",
    "dev": "export NODE_ENV=dev && nodemon -L --watch src --exec ts-node ./src/server.ts",
    "build": "export NODE_ENV=prod && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.21",
    "@types/validator": "^13.11.7",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mysql": "^2.18.1",
    "nodemon": "^3.0.2",
    "reflect-metadata": "^0.1.14",
    "typeorm": "^0.3.17",
    "typescript": "^5.3.3"
  }
}
